"use client";import{useState}from"react";export function VerificationActions({id}:{id:string}){const[busy,setBusy]=useState(false);async function review(status:"approved"|"rejected"){const note=window.prompt(status==="approved"?"Optional reviewer note":"Reason for rejection")||"";setBusy(true);const r=await fetch("/api/admin/verification",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status,note})});setBusy(false);if(r.ok)window.location.reload();}return <div className="flex gap-2"><button disabled={busy} onClick={()=>review("approved")} className="rounded-full bg-[#135d44] px-3 py-2 text-xs font-semibold text-white">Approve</button><button disabled={busy} onClick={()=>review("rejected")} className="rounded-full border border-black/10 px-3 py-2 text-xs font-semibold">Reject</button></div>}
