"use client";import{useState}from"react";export function RoleSelect({userId,value,editable}:{userId:string;value:string;editable:boolean}){const[v,setV]=useState(value);async function change(next:string){const old=v;setV(next);const res=await fetch("/api/admin/roles",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({userId,role:next})});if(!res.ok)setV(old)}if(!editable)return <span className="capitalize">{v.replaceAll("_"," ")}</span>;return <select value={v} onChange={e=>change(e.target.value)} className="rounded-xl border border-black/10 px-2 py-2 text-xs"><option value="user">User</option><option value="moderator">Moderator</option><option value="administrator">Administrator</option><option value="super_administrator">Super Administrator</option></select>}
