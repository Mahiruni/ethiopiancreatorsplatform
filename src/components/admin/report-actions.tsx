"use client";import{useState}from"react";export function ReportActions({id,value}:{id:string;value:string}){const[v,setV]=useState(value);async function change(next:string){const old=v;setV(next);const r=await fetch("/api/admin/reports",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id,status:next})});if(!r.ok)setV(old)}return <select value={v} onChange={e=>change(e.target.value)} className="rounded-xl border border-black/10 px-3 py-2 text-xs"><option value="open">Open</option><option value="reviewing">Reviewing</option><option value="resolved">Resolved</option><option value="dismissed">Dismissed</option></select>}
