"use client";import {useState}from"react";import{AuthShell}from"@/components/auth/auth-shell";import{createClient}from"@/lib/supabase/client";import{publicEnv,hasSupabaseEnv}from"@/lib/env";export default function Forgot(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(!hasSupabaseEnv){setMsg("Connect Supabase first.");return;}const email=String(new FormData(e.currentTarget).get("email"));const{error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${publicEnv.NEXT_PUBLIC_APP_URL}/auth/callback?next=/reset-password`});setMsg(error?.message||"Password reset email sent.");}return <AuthShell><form onSubmit={submit} className="card w-full max-w-md p-7"><h1 className="text-3xl font-semibold">Reset your password</h1><p className="mt-2 text-sm text-[#68716c]">We’ll send a secure recovery link.</p>{msg&&<p className="mt-4 rounded-xl bg-[#f1f3ef] p-3 text-sm">{msg}</p>}<input name="email" type="email" required placeholder="you@example.com" className="mt-6 w-full rounded-2xl border border-black/10 px-4 py-3"/><button className="mt-4 w-full rounded-full bg-[#135d44] px-5 py-3 text-sm font-semibold text-white">Send reset link</button></form></AuthShell>}
