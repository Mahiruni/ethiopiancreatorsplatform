import {NextResponse}from"next/server";import{z}from"zod";import{createClient}from"@/lib/supabase/server";
const schema=z.object({note:z.string().trim().min(20).max(1500)});
export async function POST(request:Request){const supabase=await createClient();const{data:c}=await supabase.auth.getClaims();const uid=c?.claims?.sub;if(!uid)return NextResponse.json({error:"Unauthorized"},{status:401});const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Please provide at least 20 characters of context."},{status:400});const{data:profile,error:profileError}=await supabase.from("profiles").select("id").eq("user_id",uid).single();if(profileError||!profile)return NextResponse.json({error:"Profile not found."},{status:404});const{error}=await supabase.from("verification_requests").insert({profile_id:profile.id,reason:parsed.data.note});if(error)return NextResponse.json({error:error.code==="23505"?"You already have an open verification request.":"Unable to submit request."},{status:error.code==="23505"?409:500});return NextResponse.json({ok:true});}
