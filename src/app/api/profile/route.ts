import {NextResponse}from"next/server";import{z}from"zod";import{createClient}from"@/lib/supabase/server";
const schema=z.object({display_name:z.string().trim().min(1).max(80).optional(),bio:z.string().max(240).nullable().optional(),category:z.string().max(60).nullable().optional(),locale:z.enum(["en","am"]).optional(),is_public:z.boolean().optional(),analytics_enabled:z.boolean().optional(),notification_preferences:z.object({sales:z.boolean(),account:z.boolean()}).optional()});
export async function PATCH(request:Request){const supabase=await createClient();const{data:c}=await supabase.auth.getClaims();const uid=c?.claims?.sub;if(!uid)return NextResponse.json({error:"Unauthorized"},{status:401});const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid settings"},{status:400});const{data,error}=await supabase.from("profiles").update(parsed.data).eq("user_id",uid).is("deleted_at",null).select().single();if(error)return NextResponse.json({error:"Unable to save settings."},{status:500});return NextResponse.json({profile:data});}
