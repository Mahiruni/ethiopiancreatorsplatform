"use client";import{useState}from"react";import{AuthShell}from"@/components/auth/auth-shell";import{createClient}from"@/lib/supabase/client";export default function Reset(){const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const password=String(new FormData(e.currentTarget).get("password"));if(password.length<8){setMsg("Use at least 8 characters.");return;}const{error}=await createClient().auth.updateUser({password});if(error){setMsg(error.message);return;}window.location.assign("/dashboard");}return <AuthShell><form onSubmit={submit} className="card w-full max-w-md p-7"><h1 className="text-3xl font-semibold">Choose a new password</h1>{msg&&<p className="mt-4 rounded-xl bg-[#f1f3ef] p-3 text-sm">{msg}</p>}<input name="password" type="password" minLength={8} required placeholder="New password" className="mt-6 w-full rounded-2xl border border-black/10 px-4 py-3"/><button className="mt-4 w-full rounded-full bg-[#135d44] px-5 py-3 text-sm font-semibold text-white">Update password</button></form></AuthShell>}
