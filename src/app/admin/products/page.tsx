import{requireRole}from"@/lib/supabase/auth";export default async function Products(){const{supabase}=await requireRole(["administrator","super_administrator"]);const{data}=await supabase.from("products").select("id,name,price,currency,kind,is_active,created_at,profiles(username)").is("deleted_at",null).order("created_at",{ascending:false}).limit(300);return <div><h1 className="text-3xl font-semibold tracking-[-.04em]">Products</h1><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{(data||[]).map((p:any)=><article key={p.id} className="card p-5"><div className="flex justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-[.1em] text-[#7c857f]">{p.kind}</div><h2 className="mt-1 font-semibold">{p.name}</h2></div><span className="text-xs font-bold">{p.is_active?"LIVE":"DRAFT"}</span></div><div className="mt-5 flex justify-between text-sm"><span>/{p.profiles?.username}</span><b>{p.currency} {Number(p.price).toLocaleString()}</b></div></article>)}</div></div>}
