import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { MarketingPage } from "@/components/marketing/page-shell";
import { demoProfiles } from "@/lib/demo-data";
export default function Explore(){return <MarketingPage eyebrow="Explore" title="A few fictional profiles. Many real possibilities." intro="These profiles are development seed/demo content only. Production discovery reads public profiles from PostgreSQL."><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{demoProfiles.map(p=><Link href={`/${p.username}`} key={p.username} className="card p-6 transition hover:-translate-y-1 hover:shadow-lg"><div className="flex items-start justify-between"><div className="grid size-14 place-items-center rounded-full text-sm font-bold text-white" style={{background:p.accent}}>{p.display_name.split(" ").map(v=>v[0]).join("").slice(0,2)}</div><ArrowUpRight size={18}/></div><div className="mt-8 text-xs font-bold uppercase tracking-[.12em] text-[#135d44]">{p.category}</div><h2 className="mt-2 text-xl font-semibold">{p.display_name}</h2><p className="mt-2 text-sm leading-6 text-[#68716c]">{p.bio}</p><div className="mt-5 text-xs text-[#8a918d]">linqo.app/{p.username}</div></Link>)}</div></MarketingPage>}
